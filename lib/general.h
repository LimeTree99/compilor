/*! \file general.h
 \brief For basic utility.
*/

typedef enum{false=0, true=1}bool;

#define BUFF_SIZE 100   //!Buffer size for file input
                        //currently no safeguard if token is longer than BUFF_SIZE